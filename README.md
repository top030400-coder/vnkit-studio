# VNKit Studio customer releases

This public repository contains only the customer `Distribution` build and its
`stable` auto-update manifest. Internal development builds are kept locally and
must never be uploaded to this repository or its GitHub Releases.
